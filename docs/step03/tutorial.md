# Step 3: Agent Loop - 状态机与对话管理

> 目标：构建 Agent Loop 核心，管理对话状态和历史。
> 
> 代码量：约 250 行

## 本节收获

- Agent Loop 状态机设计
- 对话历史管理
- WebSocket 实时通信

## 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    Agent Loop 状态机                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌─────────┐     ┌───────────┐     ┌─────────────┐        │
│   │  IDLE   │────▶│ THINKING  │────▶│ TOOL_CALLING│        │
│   │ (空闲)  │◄────│  (思考中)  │◄────│  (调用工具)  │        │
│   └────┬────┘     └─────┬─────┘     └──────┬──────┘        │
│        │                │                    │              │
│        │                └────────────────────┘              │
│        │                         │                          │
│        │                    ┌────┴────┐                     │
│        │                    │RESPONDING│                     │
│        │                    │ (响应中) │                     │
│        │                    └────┬────┘                     │
│        └─────────────────────────┘                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 编译运行

```bash
cd src/step03
mkdir build && cd build
cmake .. && make
./nuclaw_step03
```

## 测试 WebSocket

```javascript
const ws = new WebSocket('ws://localhost:8081');
ws.onmessage = e => console.log(e.data);
ws.send('Hello!');
```
