# NuClaw Step 11 配置文件

server:
  host: "0.0.0.0"
  port: 8080

llm:
  model: "gpt-4"
  timeout: 30
  temperature: 0.7

agent:
  name: "MultiAgent"
  welcome_message: "你好！我是多 Agent 协作系统"

tools:
  enabled:
    - weather
    - time
    - calculator
